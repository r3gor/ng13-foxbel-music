import { BreakpointPipe } from './breakpoint.pipe';

describe('BreakpointPipe', () => {
  it('create an instance', () => {
    const pipe = new BreakpointPipe();
    expect(pipe).toBeTruthy();
  });
});
